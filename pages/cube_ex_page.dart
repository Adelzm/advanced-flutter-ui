import 'package:flutter/material.dart';

class CubeExPage extends StatelessWidget {
  const CubeExPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}